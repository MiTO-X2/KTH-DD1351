[or(p,q), neg(p)]. % Premises

r.                 % Goal

% Proof
[
    [1, or(p,q), premise],
    [2, neg(p), premise],
    [[3, p, assumption],          % open box for first disjunct
        [4, r, copy(3)]           % invalidly try to derive r directly
    ],
    [[5, q, assumption],          % open box for second disjunct
        [6, r, copy(5)]           % invalidly try to derive r directly
    ],
    [7, r, orel(1,3,4,5,6)]       % attempt disjunction elimination
].

